# Copy to .env.local (gitignored at the repo root) and fill in.
# Not needed yet for 4-1's scaffold (map-only, no data) — wired up starting
# Week4 4-2/4-3 once the dashboard reads aois/events/exposure_stats etc.
# Values match pipeline/config.py's SUPABASE_URL / anon key from .env.
NEXT_PUBLIC_SUPABASE_URL=https://xaljckiwksyjtasvjagg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# service_role key (same value as the repo root .env's SUPABASE_SERVICE_ROLE_KEY)
# — needed by web/lib/supabase-admin.ts since Week 4-2 (aois/events writes),
# but this file never documented it until Week 4-9 noticed the gap while
# adding real auth. Server-side only (that module is `server-only`-guarded) —
# never exposed to the browser bundle, unlike the NEXT_PUBLIC_ vars above.
SUPABASE_SERVICE_ROLE_KEY=
