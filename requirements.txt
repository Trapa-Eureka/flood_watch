# PH Flood Watch — 2-3일 기술검증(스파이크) 의존성
# 스펙 §14 1단계: rasterio, GDAL, geopandas, pystac-client, torch, terratorch 설치 확인

# 지리공간 처리 (rasterio는 macOS wheel에 GDAL을 정적으로 번들 — 시스템 brew GDAL과 별개)
rasterio>=1.4,<2.0
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
rio-cogeo>=5.3

# STAC 수집
pystac-client>=0.8
requests>=2.32
python-dotenv>=1.0

# ML / 모델 추론
torch>=2.4
terratorch>=1.0

# 시각화 / 유틸
matplotlib>=3.9
numpy>=1.26,<2.0
pillow>=10.4
