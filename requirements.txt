# PH Flood Watch — dependencies for the 2-3 day technical validation (spike)
# Spec §14 step 1: confirm rasterio, GDAL, geopandas, pystac-client, torch, terratorch install

# Geospatial processing (rasterio bundles GDAL statically in its macOS wheel — separate from system brew GDAL)
rasterio>=1.4,<2.0
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
rio-cogeo>=5.3

# STAC collection
pystac-client>=0.8
requests>=2.32
python-dotenv>=1.0

# ML / model inference
torch>=2.4
terratorch>=1.0

# Visualization / utilities
matplotlib>=3.9
numpy>=1.26,<2.0
pillow>=10.4
