# PH Flood Watch — dependencies for the 2-3 day technical validation (spike)
# Spec §14 step 1: confirm rasterio, GDAL, geopandas, pystac-client, torch, terratorch install

# Geospatial processing (rasterio bundles GDAL statically in its macOS wheel — separate from system brew GDAL)
rasterio>=1.4,<2.0
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
rio-cogeo>=5.3

# STAC collection
pystac-client>=0.8
requests>=2.32
python-dotenv>=1.0

# Cloudflare R2 (S3-compatible) — pipeline/stac_client.py upload_raw_scene_to_r2()
boto3>=1.34

# ML / model inference
torch>=2.4
terratorch>=1.0
einops>=0.8       # used directly by pipeline/inference/prithvi_inference.py (also a terratorch transitive dep)

# Serverless GPU deployment (Week 2) — needs `modal setup` (browser OAuth) before use.
# NOTE: the actual Modal container image (pipeline/inference/modal_app.py) pins its
# OWN deps separately (torch/terratorch/rasterio/etc run remotely on Modal's Linux
# containers, not this local Python env) — this line is only for the local `modal`
# CLI/SDK used to deploy/invoke it.
modal>=1.0

# Visualization / utilities
matplotlib>=3.9
numpy>=1.26,<2.0
pillow>=10.4
